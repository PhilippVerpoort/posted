name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    name: Tests, Lint, Docs
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Sync dependencies for unit tests
        run: |
          uv sync

      - name: Run unit tests
        run: |
          uv run python -m unittest discover -v

      - name: Sync dependencies for ruff
        run: |
          uv sync --group dev

      - name: Ruff lint
        run: |
          uv run ruff check .

      - name: Ruff format check
        run: |
          uv run ruff format --check .

      - name: Sync dependencies
        run: |
          uv sync --group docs

      - name: Register docs kernel
        run: |
          uv run scripts/register_docs_kernel.py

      - name: Execute Jupyter notebooks
        run: |
          BUILD_POSTED_DOCS=1 uv run jupytext --to notebook --execute docs/data/*.py

      - name: Build documentation
        run: |
          uv run mkdocs build --strict
